LABEL_NORM_MEAN = 0.0  # 0.14
LABEL_NORM_STD = 1.0  # 0.35
delta = 32  # how many labels to take for pose delta generation
event_bins = 1
transformer_out_features = 128
mc = 350  # binned event image size, max is around 345
BATCH_SIZE = 800